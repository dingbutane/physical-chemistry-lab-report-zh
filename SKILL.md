---
name: physical-chemistry-lab-report-zh
description: Use this skill when writing, revising, or finishing Chinese physical chemistry lab reports from experiment handouts, template reports, raw data, plots, literature values, and Word documents. It covers data processing, matplotlib plotting, DOCX editing, formula/variable formatting, discussion writing, thought-question answers, references, and PDF quality checks.
metadata:
  short-description: 中文物化实验报告撰写、制图、排版与 QA
---

# 中文物化实验报告

## 适用场景

用于中文物理化学实验报告，尤其是用户提供实验讲义、模板报告、原始数据图片或 Word 文档，并希望生成或修改 `.docx` 终稿时。

优先调用相关能力：

- Word 文档：使用 `docx` 或 Documents 相关能力。
- 图表：使用 `matplotlib`，必要时结合 `numpy`、`pandas`、`PIL`。
- 语言润色：使用 `humanizer-zh`，让讨论和结论像学生自己写出的实验分析。
- 文献/标准数据：若需要最新网页、NIST、标准相图或外部资料，必须联网核查并在报告中列出来源。

## 核心原则

- 先学习材料，再写报告：先读实验讲义和 `Templates` 中用户以前的报告，模仿其标题层级、字体、表格、公式和引用风格。
- 不覆盖用户手动修改过的终稿：对现有报告做局部修改时，另存为带后缀的新版本，如 `_讨论版_v2.docx`。
- 数据处理必须可复核：正式写入前，把从图片或表格识别出的关键数据列给用户核对；用户指出的改值要优先生效。
- 图表服务于报告，不追求炫技：用清晰的坐标、单位、图例和高清 PNG。若用户要求 Origin 而不可用，优先用 matplotlib 复现相同信息。
- 公式和物理量要像实验报告：物理量用斜体，必要处用上下标或内嵌公式，避免出现 `p_t`、`x_液`、`nD` 这类裸文本写法。
- Word 结果必须 QA：修改后尽量用 LibreOffice 转 PDF，并渲染关键页检查图、表、公式、分页和参考文献。

## 工作流

1. 盘点文件
   - 用 `rg --files` 或 PowerShell 列出讲义、模板、原始数据、已有报告和图片。
   - 明确当前应修改的基准文件，尤其确认是否为用户手动调整后的终稿。

2. 学习模板
   - 查看 `Templates` 中相近实验报告。
   - 记录正文格式、标题命名、表格字号、表头是否加粗、公式呈现方式、图注表注、参考文献格式。
   - 后续输出尽量贴近用户已有风格，不另起一套版式。

3. 处理数据
   - 从讲义或数据表提取计算公式、校正公式和实验要求。
   - 从原始数据图片或 Word 表格整理数据，并保留原始读数列。
   - 需要平均值时，在表中列出原始读数和平均值。
   - 对折射率-组成工作曲线，可用线性插值或平滑拟合；报告中至少给出一个线性插值示例计算。
   - 大气压、温度等校正需要参考模板已有写法，并代入本实验数值。

4. 绘图
   - 用 matplotlib 生成高清 PNG，默认输出到 `figures/`。
   - 坐标轴写清物理量和单位，如 `x(异丙醇) / %`、`T / °C`。
   - 若绘制相图，横坐标通常统一为 0-100%，并区分液相线、气相线和纯组分沸点。
   - 若进行文献对比，把文献数据与实验点叠加；外部数据如 NIST 或 VLE 汇编必须在正文和参考文献中说明来源。
   - 纯组分沸点可作为气相线与液相线交点，与实验线和文献线连接。

5. 写入 Word
   - 优先在已有 `.docx` 上局部修改，保持用户手动调整过的格式。
   - 正文中文用宋体，英文和数字用 Times New Roman；表格字号按模板，常见为五号。
   - 表头按模板处理；若用户要求，表头不要加粗，表头底色为白色。
   - 插图居中，宽度与上下文图片一致；图注、表注编号连续。
   - 表格不要随意重建全篇，只修改必要区域。

6. 公式与变量格式
   - 物理量如 *T*、*p*、*x*、*y*、*n*、*k*、*E* 通常用斜体。
   - 下标和上标使用 Word 格式或 OMML 公式，例如 `ΔT` 的压力校正下标、`n₂`、`T₁`、`10⁻⁴ °C⁻¹`。
   - 对独立计算式，优先插入居中公式；对句中变量，使用内嵌格式化文本。
   - 避免用下划线表示下标，除非是在代码或文件名中。

7. 讨论与思考题
   - 讨论要紧扣实验数据与文献差异，不只泛泛列误差。
   - 先说明趋势是否一致，再写具体偏差，如最低恒沸点温度差、组成偏移方向、气液相线的系统偏移。
   - 误差来源要和偏差方向对应，例如样品暴露挥发且环己烷更易挥发，可能导致样品异丙醇相对含量升高、实验曲线右移。
   - 可讨论回流滞后、轻微分馏、取样挥发、平衡不足、折射率测温偏差、气相样品量小、露茎/温度梯度未校正等。
   - 思考题按模板格式作答，语言自然，避免机械的三段式和空泛总结。

8. 质量检查
   - 保存新版本后，用 LibreOffice 转 PDF。
   - 渲染关键页为 PNG，检查：
     - 图表是否清晰、坐标和图例是否完整。
     - 表格是否跨页错乱，表头和字号是否符合要求。
     - 公式、斜体、上下标是否正常显示。
     - “思考题”和“参考文献”是否在合适位置。
   - 最终回复用户时说明最新文件路径、主要改动和是否完成 PDF 检查。

## 常用实现提示

- 在 Windows 上，Codex bundled Python 常见于：
  `%USERPROFILE%\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`
- LibreOffice 路径因机器而异，先用 `Get-Command soffice`、`where soffice` 或常见安装目录查找。
- 若 matplotlib、python-docx 或 PDF 渲染库缺失，先运行导入测试；安装依赖或联网查询需要按系统授权流程请求用户批准。
- 对 Word 复杂格式，`python-docx` 可处理段落、表格、图片和基本上下标；更复杂公式可直接写入 OMML。

## 完成时的汇报格式

简洁说明：

- 最新 `.docx` 文件路径。
- 若有，检查用 PDF 或关键图片路径。
- 已完成的核心修改，例如数据表、图、讨论、思考题、参考文献、公式上下标。
- 若某一步无法验证，要明确说明原因。
